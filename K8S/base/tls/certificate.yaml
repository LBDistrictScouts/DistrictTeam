apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: district-team-tls
  namespace: district-team
spec:
  secretName: district-team-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - district-team.example.com
