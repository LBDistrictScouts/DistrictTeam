apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: district-team-test
namePrefix: test-

resources:
  - ../../../operations

labels:
  - pairs:
      environment: test
    includeSelectors: true
    includeTemplates: true

patches:
  - path: migrate-job-patch.yaml
